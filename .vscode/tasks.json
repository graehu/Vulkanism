{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "SFML": "../Libs/SFML-2.5.1",
    "Vulkan": "../Libs/Vulkan/1.1.121.1/x86_64",
    "GLFW": "../Libs/glfw-3.3",
    "tasks": [
        {
            "type": "shell",
            "label": "Build All",
            "command": "/usr/bin/g++",
            "args": [
                "$SFML",
                "$VULKAN",
                "$GLFW",
                "-g",
                "$SOURCE",
                "-o",
                "Vulkanism.out",
                "$LINKS",
                "-v"
            ],
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "VULKAN": "-I${config:tasks.Vulkan}/include/ -L${config:tasks.Vulkan}/lib/",
                    "SFML": "-I${config:tasks.SFML}/include/ -L${config:tasks.SFML}/lib/",
                    "GLFW": "-I${config:tasks.GLFW}/include/ -L${config:tasks.GLFW}/lib/",
                    "SOURCE": "main.cpp",
                    "LINKS": "-lsfml-window -lsfml-system -lsfml-graphics -lvulkan -lglfw"
                }
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}